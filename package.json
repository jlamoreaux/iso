{
  "name": "iso",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "6.1.0"
  },
  "scripts": {
    "watch-ts": "lerna run watch-ts",
    "start": "lerna run start",
    "dev": "lerna run dev --stream",
    "build": "lerna run build",
    "test": "lerna run test"
  }
}